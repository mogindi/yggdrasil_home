groups:
- name: NetworkRoutes
  rules:
  - alert: NetworkRoutesMismatch
    expr: network_routes_all_healthy == 0
    for: 5m
    labels:
      severity: critical
    annotations:
      summary: One or more routing tables are missing expected routes
